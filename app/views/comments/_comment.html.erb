<div class="card mb-2">
  <div class="card-body">
    <p class="card-text"><%= comment.content %></p>
    <p class="card-text"><small class="text-muted">Commented by <%= comment.user.email %> on <%= comment.created_at.strftime("%B %d, %Y") %></small></p>
  </div>
</div>
